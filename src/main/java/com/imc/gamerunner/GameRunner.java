package com.imc.gamerunner;
/**
 * An interface representing a generic game runner.
 * Classes implementing this interface should provide the functionality to run a game.
 */
public interface GameRunner {
    /**
     * Runs the game, executing the necessary logic for gameplay.
     */
    void runGame();
}
